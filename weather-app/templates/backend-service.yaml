apiVersion: v1
kind: Service
metadata:
  name: weather-app-backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 8000
  selector:
    app: weather-backend